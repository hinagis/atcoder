use proconio::{input as I, fastout as F, marker::Chars as C};

#[F]
fn main() {
    I! {q: usize}
    for _ in 0..q {
        I! {
            h: usize,
            w: usize,
            s: [C; h]
        }
        let mut c = 0;
        println!("{}", c);
    }
}
